#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "main.h"

/**
 * main - entry point
 *
 * Return: generated password
 */

int main(void)
{
	char c;
	int x;

	srand(time(0));
          for ( i=0, i<20, i++)
	  {c = ++i}
	  printf("le mot de pass",c);}
}
